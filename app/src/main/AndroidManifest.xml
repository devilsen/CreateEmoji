<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="devilsen.me.emojicreator">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".EmojiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".sample.emojilist.MainActivity"
            android:theme="@style/MainActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sample.createemoji.CreateActivity"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".sample.user.login.LoginActivity"
            android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".sample.search.SearchEmojiActivity" />

        <activity android:name=".sample.other.AboutActivity" />

        <activity android:name=".sample.other.FeedbackActivity" />

        <activity android:name=".sample.uploadimage.UploadActivity" />

        <meta-data
            android:name="UMENG_APPKEY"
            android:value="58b51eb76e27a4166a000e32" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="wandoujia" />

    </application>

</manifest>